# Benchmark Code 使用说明

本文档介绍如何使用本框架进行地震数据（SEG-Y / NPY / MAT）的插值与去噪训练，以及推理评估。

---

## 一、项目简介

本框架是一个基于 PyTorch 的模块化基准模板，支持：
- **数据格式**：`.sgy`（SEG-Y）、`.npy`、`.mat`
- **训练模式**：单文件自监督（通过掩码生成样本对）、双文件有监督（输入+标签）
- **并行训练**：单 GPU 与 DDP 多卡分布式训练
- **推理评估**：完整 shot 重建、归一化域 metric、可视化与可选的 npy 输出（原始幅度域）

所有组件通过 **YAML 配置 + Registry 工厂** 模式管理，新增模型/损失/metric 无需修改训练脚本。

---

## 二、目录结构

```
benchmark_code/
├── tools/                            # 数据工具（I/O、预处理、patchify）
│   ├── array_io.py                   # 统一数据加载（sgy/npy/mat）
│   ├── preprocessing.py              # 归一化、球面发散校正、掩码、加噪
│   ├── patching.py                   # patchify / unpatchify
│   └── segy_read.py                  # SEG-Y 专用读取
├── model/                            # 模型定义与注册中心
│   ├── registry.py                   # MODEL_REGISTRY
│   └── ...                           # 各模型实现（unet、res_unet 等）
├── utils/                            # 训练基础设施
│   ├── train_utils.py                # 训练/评估循环、DDP、checkpoint
│   ├── metrics.py                    # metric 注册与实现（SNR/PSNR/SSIM 等）
│   ├── losses.py                     # 损失函数注册
│   ├── inference_utils.py            # 推理工具（patch推理、per-shot metric、可视化）
│   ├── datasets.py                   # 数据集注册
│   ├── visualization.py              # 画图工具
│   └── logger.py                     # 训练日志（CSV + PNG）
├── configs/                          # YAML 配置文件（一实验一文件）
│   ├── interpolation/                # 插值任务配置
│   └── coherent_noise_attenuation/   # 去噪任务配置
├── scripts/                          # 入口脚本
│   ├── interpolation/                # 插值任务脚本
│   │   ├── train_*.py
│   │   ├── train_*.sh
│   │   ├── inference_*.py
│   │   └── inference_*.sh
│   └── coherent_noise_attenuation/   # 去噪任务脚本
│       ├── train_*.py
│       └── train_*.sh
├── results/                          # 实验输出（自动创建，Git 忽略）
│   └── <exp_name>/
│       ├── checkpoints/              # .pt 权重文件（含 best.pt）
│       ├── logs/                     # loss/metric CSV、训练日志文本
│       └── visualizations/           # 训练过程可视化 PNG
└── memory/                           # 项目记忆（设计决策、更新记录）
```

---

## 三、核心组件介绍

### 3.1 数据加载（`tools/array_io.py`）

统一入口 `load_volume(data_cfg)`，根据文件扩展名自动分发：
- `.npy` → `numpy.load`
- `.mat` → `scipy.io.loadmat`
- `.sgy` / `.segy` → `segyio`（返回 `(n_shots, n_traces, n_time)`）

**`.mat` 文件注意事项**：当 `.mat` 中存在多个非内部变量且未指定 `key` 时，会抛出 `ValueError` 并列出候选变量名，强制用户显式指定：
```yaml
mat:
  path: /path/to/data.mat
  key: shots   # 必须显式指定变量名
```

训练脚本中不再直接调用格式相关的读取函数，统一使用 `load_volume`。

### 3.2 预处理（`tools/preprocessing.py`）

所有函数均为纯 numpy、向量化实现，作用于 `(n_shots, n_traces, n_time)`：

| 函数 | 作用 | 可 skip |
|------|------|---------|
| `spherical_divergence_correction` | 沿时间轴乘以 `(t + t0) ** power`；默认 `power=1.0`（3D 振幅补偿）| 是 |
| `normalize` | minmax / max_abs / mean_std，支持 percentile clip | 是 |
| `denormalize` | `normalize` 的逆操作 | — |
| `inverse_spherical_divergence_correction` | 球面发散校正的逆操作 | — |
| `mask_traces` | 均匀/随机/连续掩码 trace | 是 |
| `add_noise` | 高斯/泊松噪声（SNR 控制）| — |

**`skip` 机制**：在 YAML `preprocess.skip` 列表中填入步骤名即可跳过。例如：
```yaml
preprocess:
  skip: ["spherical_divergence_correction", "normalize"]
```

**归一化与反归一化**

`normalize(shots, mode, per, clip_percentile)` 返回一个二元组 `(normalized, stats)`：

- `stats` 中存储的键取决于 `mode`：
  - `minmax` → `"min"`、`"max"`
  - `max_abs` → `"max_abs"`
  - `mean_std` → `"mean"`、`"std"`
- 若启用了 `clip_percentile`，还会额外包含 `"clip_threshold"`。
- `per` 参数决定了统计量的形状：
  - `global` → 标量
  - `shot` → `(n_shots,)`
  - `trace` → `(n_shots, n_traces)`

`denormalize(normalized, stats, mode, per)` 是 `normalize` 的严格逆操作，**要求调用者保留 `mode` 和 `per`**（这两个参数目前不存入 `stats`，需单独传递）。示例：

```python
from tools.preprocessing import normalize, denormalize

norm_shots, stats = normalize(shots, mode="max_abs", per="shot")
# ... 训练或推理 ...
orig_shots = denormalize(norm_shots, stats, mode="max_abs", per="shot")
```

**配对训练中的归一化**

配对训练（去噪 / 通用配对）使用 input 的统计量来归一化 target，确保两卷数据处于同一尺度。`override_stats` 现在支持所有三种 scope：
- `global`：两卷共用同一组标量统计量
- `shot`：每炮使用各自的统计量（适用于炮间能量差异较大的场景）
- `trace`：每道使用各自的统计量

```python
input_shots, in_stats = normalize(input_shots, mode="max_abs", per="shot")
target_shots, _ = normalize(target_shots, mode="max_abs", per="shot", override_stats=in_stats)
```

在推理脚本中，通常会在**归一化域**计算指标，随后调用 `denormalize` + `inverse_spherical_divergence_correction` 恢复原始幅度域，仅用于可视化或 `.npy` 输出。

### 3.3 Patchify（`tools/patching.py`）

- `patchify_uniform`：均匀网格 + 重叠，可逆
- `unpatchify_uniform`：重叠区域取平均，重建完整 shot
- `patchify_random`：随机采样，不可逆（仅用于训练）

### 3.4 模型注册（`model/registry.py`）

通过 `@register_model("name")` 装饰器注册模型，YAML 中通过 `model.type` 指定：
```yaml
model:
  type: unet
  params: { in_channels: 1, out_channels: 1, base_channels: 32, depth: 4 }
```

新增模型只需：
1. 在 `model/` 下创建文件，定义 `nn.Module` 并装饰 `@register_model("my_model")`
2. 在 `model/__init__.py` 中 import 该模块（触发注册）
3. YAML 中写 `type: my_model`

### 3.5 Metric（`utils/metrics.py`）

支持 `mse`、`rmse`、`mae`、`snr`、`psnr`、`ssim`。注册方式与模型相同：
```yaml
metrics:
  - name: snr
    params: { reduction: per_sample }
  - name: psnr
    params: { data_range: 1.0, reduction: per_sample }
```

> 注：`psnr` 的 `data_range` 指**最大振幅**（`max |x|`），不是峰峰值。对 `max_abs` 归一化到 `[-1, 1]` 的数据应填 `1.0`；`ssim` 的 `data_range` 才是峰峰值（`max - min`），对 `[-1, 1]` 数据填 `2.0`。
>
> 若使用 `mean_std` 归一化，数据没有固定有界范围。推理脚本会**自动从实际 target 数据推断** `psnr_peak = max|target|` 和 `ssim_data_range = max(target) - min(target)`。此时 YAML 中显式指定的 `data_range` 仍会覆盖自动推断值。若 target 为常数零，自动回退到 `1.0` 以避免除零错误。

### 3.6 推理工具（`utils/inference_utils.py`）

- `inference_on_shots`：patchify → batched forward → unpatchify
- `compute_shot_metrics`：逐 shot 计算 metric（vectorized numpy + SSIM per-shot）
- `select_random_shots` / `save_shot_visualizations`：随机选 shot 并保存 4-panel PNG

### 3.7 插值模型一览

本框架目前支持 **12 个插值模型**，分为三类管线：

#### (A) U-Net 族（CNN baseline）— C=1 patch 输入

| 注册名 | 文件 | 说明 |
|--------|------|------|
| `unet` | `model/interpolation/unet.py` | 标准 2D U-Net |
| `res_unet` | `model/interpolation/res_unet.py` | 残差 U-Net |
| `atten_unet` | `model/interpolation/atten_unet.py` | 注意力门 U-Net |
| `dncnn` | `model/interpolation/dncnn.py` | DnCNN（残差学习） |
| `unet_plusplus` | `model/interpolation/unet_plusplus.py` | U-Net++ |

#### (B) Gated Transformer 族（token 管线）— 1D token + 坐标 + mask 输入

| 注册名 | 文件 | 说明 |
|--------|------|------|
| `gated_transformer_v9` | `model/interpolation/gated_transformer_v9.py` | Qwen3 风格 gated attention，self-attn 编解码 |
| `gated_transformer_v9_encdec` | `model/interpolation/gated_transformer_v9_encdec.py` | V9 + cross-attn decoder |
| `gated_transformer_v11` | `model/interpolation/gated_transformer_v11.py` | V9 + 可选 data embedding |
| `gated_transformer_v11_encdec` | `model/interpolation/gated_transformer_v11_encdec.py` | V11 + cross-attn decoder |

#### (C) Patch Transformer / ViT 族（patch 管线）— C=1 或 C=2 patch 输入，支持 mask channel

| 注册名 | 文件 | 说明 |
|--------|------|------|
| `trace_token_transformer_interpolator` | `model/interpolation/trace_token_transformer_interpolator.py` | 每道作为一个 token，sincos 位置编码 + Transformer encoder-decoder |
| `hf_vit_interpolator` | `model/interpolation/hf_vit_interpolator.py` | HuggingFace ViT encoder + 可学习 unpatchify head，支持预训练权重 |

**三类管线的关键差异**：

| 特性 | U-Net 管线 | Gated Transformer 管线 | Patch Transformer 管线 |
|------|-----------|----------------------|----------------------|
| **数据格式** | 2D patch `(B,1,H,W)` | 1D token `(B,L,D)` + coords | 2D patch `(B,C,H,W)` |
| **模型接口** | `forward(x)` | `forward(x, coords, time_bounds, mask)` | `forward(x)` |
| **坐标信息** | 不使用 | 6D（炮检点 + 时间边界） | 通过 mask channel（C=2） |
| **训练脚本** | `train_interpolation_unet.py` | `train_interpolation_transformer.py` | `train_interpolation_patch_transformer.py` |
| **推理脚本** | `inference_interpolation.py` | `inference_interpolation_transformer.py` | 复用 U-Net 推理脚本 |
| **额外依赖** | 无 | 无 | `transformers`（仅 hf_vit） |
| **预训练权重** | 无 | 无 | ViT 支持 HuggingFace 预训练 |

---

## 四、配置文件说明

以 `configs/interpolation/interpolation_unet.yaml` 为例，核心字段：

```yaml
experiment:
  name: interp_unet_base      # 实验名，决定输出目录
  output_dir: results
  seed: 42
  device: cuda                # 单卡时有效；DDP 时自动使用 LOCAL_RANK

data:
  # 单文件模式（三选一）
  segy:
    path: /path/to/data.sgy
    traces_per_shot: 201
    time_downsample: 1
  # npy:
  #   path: /path/to/data.npy
  # mat:
  #   path: /path/to/data.mat
  #   key: shots

  test_ratio: 0.1             # 验证集比例（patch 级别随机划分）

  # 按炮号（FFID）划分训练/验证/测试集（顺序取炮，非随机）
  # 当 shot_split 存在时，test_ratio 被忽略
  # shot_split:
  #   train: 7
  #   val: 1
  #   test: 1

  loader:
    batch_size: 512
    num_workers: 4
    pin_memory: true

preprocess:
  dt: 0.002                   # 采样间隔（秒）
  t0: 0.0
  spherical_power: 1.2        # 球面发散指数
  uniform_stride: 2           # mask_traces 均匀间隔
  normalize_mode: max_abs     # minmax / max_abs / mean_std
  normalize_scope: global     # global / shot / trace
  patch_time: 256             # patch 时间轴长度
  patch_trace: 128            # patch 道轴长度
  patch_overlap: 0.5          # 重叠比例
  max_shots: null             # 限制 shot 数（调试用）
  # skip: []                  # 跳过某些预处理步骤

model:
  type: unet
  params: { in_channels: 1, out_channels: 1, base_channels: 32, depth: 4 }

loss:
  type: mse
  params: { reduction: mean }

metrics:
  - name: snr
    params: { reduction: per_sample }
  - name: psnr
    params: { data_range: 1.0, reduction: per_sample }   # peak amplitude (max |x|)
  - name: ssim
    params: { data_range: 2.0, window_size: 11, sigma: 1.5 }  # peak-to-peak range

optim:
  type: adamw
  params: { lr: 1.0e-3, weight_decay: 1.0e-4 }

scheduler:
  type: cosine
  params: { min_lr: 1.0e-6 }

train:
  epochs: 50
  grad_clip: 1.0
  log_step: false             # false: 每 epoch 输出一次；true: 每 step 输出
  log_interval: 20            # log_step=true 时生效
  eval_interval: 1            # 每 N epoch 评估测试集
  ckpt_interval: 5            # 每 N epoch 保存 checkpoint
  vis_interval: 5             # 每 N epoch 可视化随机样本
  resume: null

log:
  log_dir: logs
  plot_interval: 5

# 推理参数（不影响训练）
inference:
  checkpoint: results/interp_unet_base/checkpoints/epoch_0049.pt
  output_dir: results/interp_unet_base/inference
  n_viz_shots: 5
  # batch_size: null    # null -> 使用 data.loader.batch_size
  # save_npy: false     # true -> 保存 pred/input/target 为 .npy
  # seed: null
  # device: null
```

**重要**：`inference` 块仅在推理时读取，不会影响训练。

---

## 五、训练

本框架有三条独立的插值训练管线，**每条管线使用不同的训练脚本**（模型接口不同），不可混用。

---

### 5.1 管线 A：U-Net 族（CNN patch 管线）

**数据**：单文件 SEG-Y/NPY/MAT，通过 `mask_traces` 生成 masked input。数据经 `patchify_uniform` 切成 2D patch `(P, 1, H, W)`。

**适用模型**：`unet`、`res_unet`、`atten_unet`、`dncnn`、`unet_plusplus`

```bash
# 单卡
python scripts/interpolation/train_interpolation_unet.py \
  --config configs/interpolation/interpolation_unet.yaml

# DDP 多卡
CUDA_VISIBLE_DEVICES=0,1 torchrun --nproc_per_node=2 \
  scripts/interpolation/train_interpolation_unet.py \
  --config configs/interpolation/interpolation_unet.yaml

# 多 seed 循环
bash scripts/interpolation/train_interpolation_unet.sh
```

**关键配置**：
```yaml
model:
  type: unet                          # 换成 res_unet / atten_unet / dncnn / unet_plusplus 即可
  params: { in_channels: 1, out_channels: 1, base_channels: 32, depth: 4 }

preprocess:
  patch_time: 256
  patch_trace: 128
  patch_overlap: 0.5
  # 该管线无 include_mask_channel，始终 C=1
```

**CLI 参数**：
```
--mask-mode {uniform,random,continuous}    # 默认 continuous
--mask-ratio FLOAT                          # 默认 0.2
--continuous-missing-traces INT             # continuous 模式下固定缺失道数
```

---

### 5.2 管线 B：Gated Transformer 族（token 管线）

**数据**：单文件 SEG-Y（必须，需道头提取炮检坐标），经 `trace_time_chunk` 沿时间轴切分成 1D token。每个 token 是一道的时间段，序列长度 `L = n_traces × n_chunks`。

**适用模型**：`gated_transformer_v9`、`gated_transformer_v9_encdec`、`gated_transformer_v11`、`gated_transformer_v11_encdec`

```bash
# 单卡
python scripts/interpolation/train_interpolation_transformer.py \
  --config configs/interpolation/interpolation_transformer.yaml

# DDP 多卡
CUDA_VISIBLE_DEVICES=0,1 torchrun --nproc_per_node=2 \
  scripts/interpolation/train_interpolation_transformer.py \
  --config configs/interpolation/interpolation_transformer.yaml

# 多 seed 循环
bash scripts/interpolation/train_interpolation_transformer.sh
```

**关键配置**：
```yaml
model:
  type: gated_transformer_v11_encdec      # 换成 v9 / v9_encdec / v11 即可
  params:
    input_dim: 256                          # 必须匹配 chunk_length
    d_model: 512
    n_heads: 8
    num_encoder_layers: 4
    num_decoder_layers: 4
    d_ff: 2048
    dropout: 0.1
    norm_type: rms
    use_rope: true
    use_coord_encoding: true
    coord_dim: 6                            # 4 空间坐标 + 2 时间边界
    encode_observed_only: true

preprocess:
  chunk_length: 256                         # 每个 token 的时间采样点数
  overlap_ratio: 0.0                        # token 之间无重叠
  # 不需要 patch_time / patch_trace / patch_overlap（使用 chunk 而非 patch）
```

**注意事项**：
- 数据**必须是 SEG-Y 格式**（需道头中的 SourceX/Y、GroupX/Y 坐标）
- 模型接收 5 个参数：`x, coords, time_bounds, mask`，与 U-Net 接口不兼容
- `encode_observed_only=true` 时 encoder 仅处理观测 token，decoder 通过 cross-attention 重建全部 token
- batch_size 通常较小（4-8），因为序列长度 `L` 可能很大

---

### 5.3 管线 C：Patch Transformer / ViT 族（patch 管线 + mask channel）

**数据**：单文件 SEG-Y/NPY/MAT，经 `patchify_uniform` 切成 2D patch。**支持 `include_mask_channel`**，可将 missing mask 作为第二通道拼接到输入。

**适用模型**：`trace_token_transformer_interpolator`、`hf_vit_interpolator`

```bash
# Trace Token Transformer
python scripts/interpolation/train_interpolation_patch_transformer.py \
  --config configs/interpolation/interpolation_trace_transformer.yaml

# HuggingFace ViT（首次运行需下载预训练权重）
python scripts/interpolation/train_interpolation_patch_transformer.py \
  --config configs/interpolation/interpolation_hf_vit.yaml

# DDP 多卡
CUDA_VISIBLE_DEVICES=0,1 torchrun --nproc_per_node=2 \
  scripts/interpolation/train_interpolation_patch_transformer.py \
  --config configs/interpolation/interpolation_trace_transformer.yaml
```

#### Trace Token Transformer 配置

```yaml
model:
  type: trace_token_transformer_interpolator
  params:
    in_channels: 2                  # include_mask_channel=true 时填 2
    trace_length: 64                # 必须匹配 patch_time
    embed_dim: 256
    encoder_depth: 6
    decoder_depth: 4
    num_heads: 8
    dropout: 0.0
    pos_encoding: sincos
    coord_dim: 1                    # 预留未来 5D 坐标扩展

preprocess:
  include_mask_channel: true        # 启用 mask channel（C: 1→2）
  patch_time: 64
  patch_trace: 32                   # token 数 = patch_trace，建议 ≤ 64
  patch_overlap: 0.5
  patch_normalize: true
```

#### HuggingFace ViT 配置

`hf_vit_interpolator` 支持 **三种加载模式**，由 `pretrained` 和 `model_name` 控制：

| 模式 | `pretrained` | `model_name` | 行为 |
|------|-------------|-------------|------|
| **HF Hub 预训练**（默认） | `true` | `"google/vit-base-patch16-224-in21k"` | 从 HuggingFace Hub 自动下载预训练权重（首次需联网，之后使用缓存） |
| **本地权重** | `true` | `"/data/models/my_vit"` | 从本地目录加载。以 `/`、`./`、`../`、`~` 开头的路径自动识别为本地路径，使用 `local_files_only=True` |
| **随机初始化** | `false` | *(忽略)* | 构建小型 `ViTConfig`（hidden=192, 4层, 3头），无需网络，适合离线测试 |

**本地权重使用示例**：

```bash
# 先从 HF Hub 下载到本地目录
huggingface-cli download google/vit-base-patch16-224-in21k \
  --local-dir /data/models/vit-base-patch16-224

# 然后在 config 中使用本地路径
```

```yaml
model:
  type: hf_vit_interpolator
  params:
    in_channels: 2

    # ── 权重加载源（三选一）────────────────────────────────────────
    # 模式1: HF Hub（默认）
    model_name: google/vit-base-patch16-224-in21k
    pretrained: true

    # 模式2: 本地权重
    # model_name: /data/models/vit-base-patch16-224
    # pretrained: true

    # 模式3: 随机初始化（离线测试）
    # pretrained: false
    # ────────────────────────────────────────────────────────────────
    patch_size: 16                                   # patch_time/trace 必须能被此整除
    freeze_encoder: false                            # true = 仅训练 channel adapter + unpatch head

preprocess:
  include_mask_channel: true
  patch_time: 64                    # 必须能被 patch_size (16) 整除
  patch_trace: 64                   # 必须能被 patch_size (16) 整除
  patch_overlap: 0.5
  patch_normalize: true
```

**位置编码自动插值**：当输入 patch 网格大小与预训练图像大小不同时（例如加载 224×224 预训练 ViT，输入 64×64 地震 patch），位置编码会自动通过 bicubic 插值适配到新尺寸。此过程在首次 forward 时自动完成，无需手动干预。

**加载模式内省**：

```python
from model.interpolation.hf_vit_interpolator import HFViTInterpolator

m = HFViTInterpolator(model_name="google/vit-base-...", pretrained=True)
print(m.loading_mode())  # "hub"

m = HFViTInterpolator(model_name="/data/my_vit", pretrained=True)
print(m.loading_mode())  # "local"

m = HFViTInterpolator(pretrained=False)
print(m.loading_mode())  # "random"
```

**`include_mask_channel` 说明**：

- `false`（默认）：输入 C=1，仅有 masked seismic data
- `true`：输入 C=2，channel 0 = masked data，channel 1 = binary mask（missing=1, observed=0）
- 实现方式：patchify + normalize 后检测零值 `(input == 0)`，构造 mask 并 concat
- 安全性：`mask_traces` 将缺失 trace 置零，`max_abs` 归一化和 `patch_normalize` 均保留零值
- target 始终 C=1（干净全量地震数据）

**CLI 参数**：与管线 A 相同（`--mask-mode`、`--mask-ratio`、`--continuous-missing-traces`）。

---

### 5.4 双文件有监督（去噪 / 通用配对）

数据：两个卷（输入 + 标签），分别读取后做同步预处理。

```bash
python scripts/coherent_noise_attenuation/train_denoise_res_unet.py --config configs/coherent_noise_attenuation/denoise_res_unet.yaml
```

或者使用通用配对脚本：
```bash
python scripts/interpolation/train_paired_unet.py --config configs/interpolation/paired_unet.yaml
```

### 5.5 DDP 多卡训练

```bash
CUDA_VISIBLE_DEVICES=0,1 torchrun --nproc_per_node=2 scripts/interpolation/train_interpolation_unet.py --config configs/interpolation/interpolation_unet.yaml
```

- `nproc_per_node` 必须与可见 GPU 数量一致
- DDP 模式下 `experiment.device` 被忽略，自动使用 `LOCAL_RANK`
- 只有 rank 0 会保存 checkpoint / 可视化 / 日志

### 5.6 多 seed 循环训练（bash 启动器）

修改 `scripts/interpolation/train_interpolation_unet.sh` 顶部的 `CUDA_VISIBLE_DEVICES`、`N`、`START_SEED`，然后：

```bash
bash scripts/interpolation/train_interpolation_unet.sh
```

该脚本会自动为每次循环生成临时 config，修改 `seed` 和 `experiment.name`，确保输出目录不冲突。

### 5.7 最优验证集 checkpoint

训练过程中，除按 `ckpt_interval` 保存的周期 checkpoint（`epoch_*.pt`）外，系统会自动保存 `best.pt`：
- 每当验证集 loss 低于历史最优值时，覆盖写入 `results/<exp_name>/checkpoints/best.pt`
- 包含模型参数、优化器状态、学习率调度器状态和当前 epoch
- 推理时可直接使用该权重：
  ```bash
  python scripts/interpolation/inference_interpolation.py \
    --config configs/interpolation/interpolation_unet.yaml \
    --checkpoint results/<exp_name>/checkpoints/best.pt
  ```

---

## 六、推理

三条管线各自有独立的推理脚本，需根据模型类型选择正确的脚本。

---

### 6.1 管线 A（U-Net 族）推理

与 `train_interpolation_unet.py` 配套。

```bash
# 基本用法（config 中已设置 inference.checkpoint）
python scripts/interpolation/inference_interpolation.py \
  --config configs/interpolation/interpolation_unet.yaml

# 显式指定 checkpoint
python scripts/interpolation/inference_interpolation.py \
  --config configs/interpolation/interpolation_unet.yaml \
  --checkpoint results/interp_unet_base/checkpoints/best.pt

# 启动器
bash scripts/interpolation/inference_interpolation.sh
```

### 6.2 管线 B（Gated Transformer 族）推理

与 `train_interpolation_transformer.py` 配套。使用 `inference_interpolation_transformer.py`。

```bash
# 基本用法
python scripts/interpolation/inference_interpolation_transformer.py \
  --config configs/interpolation/interpolation_transformer.yaml

# 显式指定 checkpoint
python scripts/interpolation/inference_interpolation_transformer.py \
  --config configs/interpolation/interpolation_transformer.yaml \
  --checkpoint results/interp_transformer/checkpoints/best.pt

# 启动器
bash scripts/interpolation/inference_interpolation_transformer.sh
```

**与管线 A 的关键差异**：
- 使用 `trace_time_chunk` / `trace_time_unchunk` 而非 `patchify_uniform` / `unpatchify_uniform`
- 模型前向需传 `coords`、`time_bounds`、`mask` 共 4 个参数
- 不需要 `patch_time` / `patch_trace` / `patch_overlap` 预处理参数

### 6.3 管线 C（Patch Transformer / ViT 族）推理

与 `train_interpolation_patch_transformer.py` 配套。由于模型接口为单参数 `forward(x)`，可复用管线 A 的推理脚本。

```bash
# Trace Token Transformer 推理
python scripts/interpolation/inference_interpolation.py \
  --config configs/interpolation/interpolation_trace_transformer.yaml \
  --checkpoint results/interp_trace_token_transformer/checkpoints/best.pt

# HuggingFace ViT 推理
python scripts/interpolation/inference_interpolation.py \
  --config configs/interpolation/interpolation_hf_vit.yaml \
  --checkpoint results/interp_hf_vit_base/checkpoints/best.pt
```

**注意事项**：
- 推理脚本会自动从 config 读取 `include_mask_channel`，并构造对应的 mask channel
- `pretrained=true` 时需要能访问 HuggingFace（或已缓存的权重）

---

### 6.4 推理流程（通用）

1. 读取配置与模型权重
2. `load_volume` 加载原始数据（支持 `.sgy`/`.npy`/`.mat`）
3. **正向预处理**：球面发散校正 → 归一化 → mask_traces（生成输入）
4. **推理**：`patchify_uniform`（或 `trace_time_chunk`）→ 模型分 batch 前向 → `unpatchify_uniform`（或 `trace_time_unchunk`）
5. **在归一化域计算 metric**（`pred_norm` vs `shots_norm`）
6. **逆向预处理**：`denormalize` → `inverse_spherical_divergence_correction`
7. 保存 `metrics_per_shot.csv`、`metrics_summary.json`
8. 可选：保存反归一化后的 `pred/input/target` 为 `.npy`
9. 可视化（原始幅度域）PNG

### 6.5 输出文件

```
results/<exp_name>/inference/
├── metrics_per_shot.csv      # 每行一个 shot 的 metric（归一化域）
├── metrics_summary.json      # 平均 metric + 推理耗时
├── npy/                      # 当 save_npy=true 时生成（原始幅度域）
│   ├── pred_shots.npy
│   ├── target_shots.npy
│   └── input_shots.npy
└── visualizations/
    └── <exp_name>_shot_0000.png   # input | prediction | target | residual（原始幅度域）
                                     # 默认使用统一对称颜色刻度（share_scale=True），
                                     # 残差可直接与输入/预测/目标比较振幅
```

### 6.6 推理启动器

```bash
# 管线 A
bash scripts/interpolation/inference_interpolation.sh

# 管线 B
bash scripts/interpolation/inference_interpolation_transformer.sh
```

修改脚本顶部的 `CONFIG`、`CHECKPOINT` 等变量即可。空值会自动从 config 读取。

---

## 七、训练 vs 推理的预处理一致性

训练和推理使用**同一套预处理参数**（`preprocess` 块），确保输入分布一致：

| 步骤 | 训练 | 推理 |
|------|------|------|
| 球面发散校正 | 正向（乘 gain） | 正向（乘 gain） |
| 归一化 | 训练集统计量 | 独立计算 stats |
| mask_traces | 是（生成输入） | 是（生成输入） |
| 反归一化 | 无 | 有（用于可视化 / npy 输出） |
| 反球面校正 | 无 | 有（用于可视化 / npy 输出） |
| metric 计算 | 归一化域 | **归一化域**（pred_norm vs shots_norm） |

**注意**：当前推理脚本会独立在输入数据上计算归一化 stats。如果你需要**严格复用训练时的 stats**（例如跨不同数据集推理），建议将训练 stats 保存到 checkpoint 或单独 JSON，然后在推理时读取。当前版本暂未实现此功能。

---

## 八、常见问题

**Q: 如何跳过某些预处理步骤？**  
A: 在 YAML `preprocess.skip` 中列出步骤名：
```yaml
preprocess:
  skip: ["spherical_divergence_correction", "normalize"]
```

**Q: 三条插值管线有什么区别？如何选择？**
A: 参见 [3.7 节](#37-插值模型一览) 的对比表。简言之：
- **U-Net 族**：最轻量，适合快速 baseline 和小数据集
- **Gated Transformer 族**：需要 SEG-Y 坐标信息，适合大炮集 + 坐标先验
- **Patch Transformer / ViT 族**：支持 mask channel (C=2) 和预训练权重，适合迁移学习和中等数据量

**Q: 如何更换模型？**  
A: 修改 YAML 中 `model.type` 为已注册的模型名（如 `unet`、`res_unet`、`atten_unet`），无需修改训练脚本。

**Q: 训练时可视化的是完整 shot 还是 patch？**  
A: 训练时的 `visualize_random_sample` 可视化的是**单个 patch**（随机抽取）。推理时的 `save_shot_visualizations` 可视化的是**完整 shot**。

**Q: 配对训练是否必须使用 `normalize_scope: global`？**  
A: 否。配对训练现在支持 `global` / `shot` / `trace` 三种 scope。系统会用 input 的统计量通过 `override_stats` 同步归一化 target。如果炮间能量差异大，建议使用 `shot`。

**Q: `.mat` 文件加载报错 "multiple variables found" 怎么办？**  
A: 在 YAML 中显式指定 `key`，例如：
```yaml
mat:
  path: /path/to/data.mat
  key: shots
```

**Q: 推理 metric 是在哪个域计算的？**  
A: **归一化域**。推理时先在归一化域计算 metric（`pred_norm` vs `shots_norm`），随后才执行 `denormalize` + `inverse_spherical_divergence_correction`，仅用于可视化及可选的 `.npy` 输出。

**Q: 如何同时跑多个实验？**  
A: 使用 `train_interpolation_unet.sh` 的串行循环，或自行编写并行 launcher（为每组实验分配不同 GPU 和 `MASTER_PORT`）。

**Q: 如何实现按炮号划分训练/验证/测试集？**  
A: 在 YAML `data` 块中用 `shot_split` 替代 `test_ratio`：
```yaml
data:
  shot_split:
    train: 7
    val: 1
    test: 1
```
系统会按 SEG-Y 道头 `FieldRecord`（FFID）的唯一值顺序取炮：前 7 炮为训练集，第 8 炮为验证集，第 9 炮为测试集。同一炮号下的所有炮集会被完整保留在对应子集中，避免 patch 级别的数据泄露。

**Q: `best.pt` 和 `epoch_*.pt` 有什么区别？**  
A: `epoch_*.pt` 是按 `ckpt_interval` 定期保存的周期快照；`best.pt` 是在验证集 loss 达到新低时自动覆盖保存的最优模型，与周期保存互不影响。推理时建议使用 `best.pt` 以获得泛化性能最好的权重。

**Q: 可视化颜色刻度可以调整吗？**  
A: 训练和推理可视化默认启用 `share_scale=True`，即 input、prediction、target、residual 四幅子图共用同一对称颜色刻度，便于直接比较残差振幅。如需恢复各图独立的自适应刻度，可在调用 `plot_sample` 时传入 `share_scale=False`。推理脚本已按全局 0.995 分位数计算统一的 `vmin/vmax`。

**Q: `include_mask_channel` 和旧 U-Net 的 C=1 输入有什么区别？**  
A: `include_mask_channel=true` 会在 channel 维度拼接一个 binary mask（missing=1, observed=0），使模型显式知道哪些位置被遮蔽。旧 U-Net 管线不支持此功能（始终 C=1）。该功能仅在**管线 C**（`train_interpolation_patch_transformer.py`）中可用。

**Q: HF ViT 模型首次运行报错 "transformers not installed"？**  
A: 安装依赖：`pip install transformers`。如果无法联网下载预训练权重，在 config 中设置 `pretrained: false`，此时会构建一个小型随机初始化的 ViT，可离线运行测试。

**Q: Trace Token Transformer 的 `coord_dim=1` 和 Gated Transformer 的 `coord_dim=6` 有什么区别？**  
A: `coord_dim=1`（管线 C）仅使用 trace 索引做 sincos 位置编码，不依赖 SEG-Y 道头坐标。`coord_dim=6`（管线 B）使用 4 个空间坐标（sx, sy, rx, ry）+ 2 个时间边界，需要从 SEG-Y 道头提取。`coord_dim=1` 是简化版，接口预留了未来扩展到 5D 的能力。

---

## 九、扩展开发

### 新增模型
1. 在 `model/` 下创建 `my_model.py`，定义 `nn.Module` 并用 `@register_model("my_model")` 装饰
2. 在 `model/__init__.py` 中 `from . import my_model`
3. YAML 中写 `model: { type: my_model, params: {...} }`

### 新增损失 / Metric
类似模型，使用 `utils/losses.py` 的 `@register_loss` 或 `utils/metrics.py` 的 `@register_metric`。

### 新增数据格式
在 `tools/array_io.py` 的 `load_volume` 中扩展即可。

---

如有其他问题，请查阅 `memory/updates.md` 获取最新变更记录，或查看各子目录的 `README.md`。
